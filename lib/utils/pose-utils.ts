// Pose landmark smoothing & interpolation utilities
// TODO: Migrate from drawing-app/utils/poseUtils.js
//
// Functions to implement:
// - smoothLandmarks(frames, windowSize) – moving average filter
// - interpolateLandmarkFrames(frames, steps) – linear interpolation
// - smoothAndInterpolateLandmarks(frames, smoothWindow, steps) – combined
// - scaleLandmarks(landmarks, original, target) – aspect-aware scaling
// - scaleLandmarkFrames(frames, original, target) – batch scaling

export {};
