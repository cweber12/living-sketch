// SVG transform & parsing utilities
// TODO: Migrate from drawing-app/utils/svgUtils.js
//
// Functions to implement:
// - affineFrom3Points(src, dst) – compute 2D affine transform matrix
// - svgStringToImage(svgString) – convert SVG → Image object
// - getSvgSize(img) – get natural dimensions
// - getSvgDimensions(svgString) – parse width/height from SVG markup
// - addSvgClipPath(svgString, w, h, radius) – rounded corners
// - addSvgOpacityGradient(svgString, opts) – opacity mask

export {};
