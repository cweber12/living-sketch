// Canvas drawing functions for body parts
// TODO: Migrate from drawing-app/utils/drawingUtils.js
//
// Functions to implement:
// - drawTorsoSvg(ctx, img, tl, tr, bl, br) – affine quad
// - drawHeadSvg(ctx, img, leftEar, rightEar) – scaled + centered
// - drawHorizontalSegmentSvg(ctx, img, from, to, part) – arm segments
// - drawVerticalSegmentSvg(ctx, img, from, to, part) – leg segments
// - drawHandSvg(ctx, img, wrist, elbow) – hand positioning
// - drawLegSvg(...) – leg rendering
// - drawFootSvg(...) – foot rendering

export {};
