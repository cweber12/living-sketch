/* ── Shared toolbar layout & sizing tokens ─────────────────────────────────── */

/** Height of the sticky NavBar (px) */
export const NAVBAR_H = 48;

/** Height of the top toolbar in desktop mode (px) */
export const TOOLBAR_H = 52;

/** Width of the side toolbar (px) */
export const TOOLBAR_W = 64;

/** Height of the mobile bottom toolbar (px) */
export const TOOLBAR_H_MOBILE = 60;

/** Breakpoint below which the toolbar uses mobile layout (px) */
export const MOBILE_BP = 768;

/* ── Section button sizing (ToolbarSection — used by extract page) ── */
export const SECTION_PADDING = '4px 10px';
export const SECTION_PADDING_MOBILE = '4px 12px';
export const SECTION_MIN_W_MOBILE = 52;
export const SECTION_MIN_H_MOBILE = 52;
export const SECTION_GAP = 4;
export const SECTION_LABEL_SIZE = 8;
export const SECTION_LABEL_SIZE_MOBILE = 9;
export const SECTION_SIDE_PADDING = '6px 8px';
export const SECTION_SIDE_LABEL_SIZE = 8;

/* ── Action icon sizing (ActionIcon / ToolbarGroup) ──────────────────────── */
/** Min width/height of an ActionIcon button on desktop */
export const ACTION_ICON_MIN = 32;
/** Min width/height of an ActionIcon button on mobile (touch-friendly) */
export const ACTION_ICON_MIN_MOBILE = 52;
/** Font size for text inside a labeled ActionIcon button */
export const ACTION_ICON_LABELED_FONT = 10;

/* ── Dropdown panel sizing ───────────────────────────────────────────── */
export const DROPDOWN_MIN_W = 120;
export const DROPDOWN_MAX_W = 360;
export const DROPDOWN_MIN_W_SIDE = 260;

/* ── Expand tab sizing ─────────────────────────────────────────────────── */
export const EXPAND_TAB_W = 56;
export const EXPAND_TAB_H = 8;
export const EXPAND_TAB_SIDE_W = 8;
export const EXPAND_TAB_SIDE_H = 56;
